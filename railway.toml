[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && source venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.envs]
ROOT_DIR = "backend"
